import { resultadoDaMedida } from "./elements.js";

const resultado = document.querySelector("#result")


export function dados (){
    if (resultado <= 18.5){ 
        resultadoDaMedida("Você está abaixo do peso")
    } else if (resultado ){

    }
}